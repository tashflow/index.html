rules_version = '2';
// Stak Suite — require signed-in Firebase Auth users
service cloud.firestore {
  match /databases/{database}/documents {
    match /stak_companies/{companyId}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
